# Test Environment Configuration
# This file contains environment variables specifically for testing

# Authentication
JWT_SECRET=test-jwt-secret-key-for-testing-only

# Database - MongoDB URI placeholder for build phase
# This will be overridden by the actual test server URI when tests run
MONGODB_URI=mongodb://127.0.0.1:27017/iwe-test

# Disable rate limiting during tests
DISABLE_RATE_LIMIT=true

# Test environment flag
NODE_ENV=test

# Optional: Use Docker MongoDB instead of in-memory (for CI or production-like testing)
# USE_DOCKER_MONGODB=true

# Optional: Enable debug logging for MongoDB test server
# DEBUG=true