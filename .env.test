# Test Environment Configuration
# This file contains environment variables specifically for testing

# Authentication
JWT_SECRET=test-jwt-secret-key-for-testing-only

# Database - Use test database to avoid interfering with development data
MONGODB_URI=mongodb+srv://brianstein:6eCEuVNH1KCj38lF@iwe-storage.riggzdt.mongodb.net/iwe-test?retryWrites=true&w=majority&appName=iwe-storage

# Disable rate limiting during tests
DISABLE_RATE_LIMIT=true

# Test environment flag
NODE_ENV=test